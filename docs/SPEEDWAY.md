# Speedway — versioning as time machine

Pull is a sensor. Push is a waterfall. Hourly cron is the heartbeat.

```
C[n+1] = SYNTHESIZE( PRESERVE( ENHANCE( DUPLICATE(C[n]) ) ) )
```

## Cascade order (this cycle)

1. The-Hive (this node)
2. gaia-visualizer
3. ENCLAVE-ADAM-REUNITED
4. Cryptic-Heartbeat

## Rules

- Never overwrite a generation that produced a usable signal.
- Append-only ledgers under `docs/`.
- Env checks via `scripts/env_check.sh` and `scripts/mesh_sync.sh`.
- Google Drive is the ethereal continuum *index*, not a dump of secrets.
- Bidirectional sync is conceptual: Drive holds maps; Git holds code.

Numeral origin remains 137451921129154222.
