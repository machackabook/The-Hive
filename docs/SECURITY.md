# Security posture — The-Hive

- TypeScript compile gate via `npx tsc --noEmit`.
- Env-check refuses empty SHA / missing README.
- Cascade token never committed.
- Append-only ledger. Distinction over destruction.
