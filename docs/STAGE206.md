# STAGE 206

Live chat pasted the exact session `update(t)` again (2026-09-20 09:18 CDT). Session hash `beec41f1`. Living hash `7cd81012`. Klein remains runtime-only. `STAGE = 206`.

- uniforms `uTime` / `uGravity`
- `theta += (0.01 + idx * 0.002) * gravityPull`
- session switch: infinity | hamiltonian | triangular | torus
- session lerp allocates `new THREE.Vector3` by contract
- living path: uniform guards, `phi += 0.007 * toroidalWeave`, reused target

Do not promote klein into the session switch until a paste includes it.
