# The-Hive cascade

Receives `continuity-cascade` from Cryptic-Heartbeat.  
Hourly stamp + optional dispatch to gaia-visualizer.

Do not put API keys in `firebase.ts` tracked values beyond public client config already present.  
Server secrets live in Actions / local env only.
